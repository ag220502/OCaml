(* Program FOr Printing Hello World *)
(* ============================================================ *)
(** For Printing Hello World We have to declare variable *)
(* Here First we have to write let keyword and then the name of the variable *)
(* After this we have to write the data type of the variable which we want *)
(* And then we can assign the value which we want to *)
let str : string = "Hello World";;
(* Printing the Value of Variable str *)
str;;
